name: Env Example
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Use secret in env
        env:
          API_KEY: ${{ secrets.API_KEY }}
        run: echo "API key is stored in env variable"
